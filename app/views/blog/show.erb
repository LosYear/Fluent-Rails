<% title = @post.title + " | " + Fluent::Setting.title
   wiselinks_title title
   title title
%>

<%= render partial: 'layouts/header_minimized', locals: {subtitle: @post.title} %>
<%= render partial: @post.type+'_show' %>