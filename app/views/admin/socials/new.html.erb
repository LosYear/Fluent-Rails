<%
    # Defining breadcrumbs
    @breadcrumbs = [
        {:link => admin_socials_path, :title => t('backend_part.social')},
        {:title => t('backend_part.create')}
    ]

    # Defining menu
    @menu = [
        {:link => admin_socials_path, :title => t('backend_part.manage_socials'), :icon => 'list'},
    ]
%>

<div class="page-header">
  <h1><%= t('backend_part.social') %>
    <small>&nbsp;<%= t('backend_part.create') %></small>
  </h1>
</div>

<div class="form">
  <%= render 'form' %>
</div>
